<div class="myaccountblock">
  <div align="center">
    <!-- START:flash -->
    <% if flash[:notice] -%>
      <div id="notice"><%= flash[:notice] %></div>
    <% end -%>
    <!-- END:flash -->
  </div>

  <div id="site-pref-list">
    <h1  class="myaccountheader">Shipping</h1>

    <center><%= link_to 'New Shipping', :action=>:create_empty_record,:typeofrecord=>"shipping" %></center>

    <%  @shippings.each do |shipping| %>
      <div class="hr"></div>
      <table width ="100%"cellpadding="5" cellspacing="0">
        <tr class="list-table" valign="top" class="<%= cycle('list-line-odd', 'list-line-even') %>"  onclick = "window.location.href ='<%=url_for(:controller => 'shippings', :id => shipping.id,  :action => 'edit')%>' "  style = "cursor:pointer" >
                      <td align="left">
    <span class="list-title"><%= shipping.packing_slip_no %></span>
          </td>
                   <td align="left">
    <span class="list-title"><%= shipping.date_shipped %></span>
          </td>
                   <td align="left">
    <span class="list-title"><%= shipping.shipped_via %></span>
          </td>
                   <td align="left">
    <span class="list-title"><%= shipping.weight %></span>
          </td>
                   <td align="left">
    <span class="list-title"><%= shipping.cost %></span>
          </td>
          
        </tr>
      </table>
      <div align="right" class="image-delete-list" >
        <%=link_to image_tag ("Button-Delete.png", :border=>"0") , shipping,:on_click=>"", :confirm => 'Are you sure?', :method => :delete %>
      </div>
    <% end %>
    <div class="hr"></div>

  </div>
    <center><%= link_to 'New Shipping', :action=>:create_empty_record,:typeofrecord=>"shipping" %></center>
</div>
