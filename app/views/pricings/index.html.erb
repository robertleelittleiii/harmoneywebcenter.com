<div class="myaccountblock">
  <div align="center">
    <!-- START:flash -->
    <% if flash[:notice] -%>
      <div id="notice"><%= flash[:notice] %></div>
    <% end -%>
    <!-- END:flash -->
  </div>

  <div id="site-pref-list">
    <h1  class="myaccountheader">Pricing</h1>

    <center><%= link_to 'New Pricing', :action=>:create_empty_record,:typeofrecord=>"pricing" %></center>

    <%  @pricings.each do |pricing| %>
      <div class="hr"></div>
      <table width ="100%"cellpadding="5" cellspacing="0">
        <tr class="list-table" valign="top" class="<%= cycle('list-line-odd', 'list-line-even') %>"  onclick = "window.location.href ='<%=url_for(:controller => 'pricings', :id => pricing.id,  :action => 'edit')%>' "  style = "cursor:pointer" >
                      <td align="left">
    <span class="list-title"><%= pricing.product_id %></span>
          </td>
                   <td align="left">
    <span class="list-title"><%= pricing.purchase_price %></span>
          </td>
                   <td align="left">
    <span class="list-title"><%= pricing.competitor_price %></span>
          </td>
                   <td align="left">
    <span class="list-title"><%= pricing.contract_price %></span>
          </td>
          
        </tr>
      </table>
      <div align="right" class="image-delete-list" >
        <%=link_to image_tag ("Button-Delete.png", :border=>"0") , pricing,:on_click=>"", :confirm => 'Are you sure?', :method => :delete %>
      </div>
    <% end %>
    <div class="hr"></div>

  </div>
    <center><%= link_to 'New Pricing', :action=>:create_empty_record,:typeofrecord=>"pricing" %></center>
</div>
